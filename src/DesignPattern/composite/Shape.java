package DesignPattern.composite;
/*
 * composition pattern is applied when the group of object needs to be treated as a single 
 * while applying some operation to them
 */
public interface Shape {
public void draw(String fillcolor);
}
